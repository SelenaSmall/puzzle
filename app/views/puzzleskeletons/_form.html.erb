<%= form_for(@puzzleskeleton) do |f| %>
  <% if @puzzleskeleton.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@puzzleskeleton.errors.count, "error") %> prohibited this puzzleskeleton from being saved:</h2>

      <ul>
      <% @puzzleskeleton.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
